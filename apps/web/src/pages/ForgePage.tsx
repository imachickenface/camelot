import { ForgeHero } from '@/features/forge/ForgeHero';

/**
 * The Forge — the first real tool surface. For now it hosts an interactive
 * hero section; the video-making tooling will grow from here.
 */
export function ForgePage() {
  return <ForgeHero />;
}
