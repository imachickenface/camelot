node_modules
dist
.DS_Store
*.log
.vite

# Higgsfield API credentials — never commit
.env

# Uploaded portraits are user state, but keep the folder + defaults tracked.
# (Uncomment the next line if you don't want to version custom uploads.)
# src/assets/portraits/*
# !src/assets/portraits/arthur.svg
# !src/assets/portraits/vacant-silhouette.svg

# Generated audio clips (Miku/Teto) — pure output, not worth versioning.
src/assets/audio/
